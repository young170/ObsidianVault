Example of active-high connection:

```
D0 - (+LED-) - GND
```

In this example, `GND` is always LOW. Therefore, only when D0 is HIGH (has higher potential than the (-) side ([[GND]])) can the LED be lit up.